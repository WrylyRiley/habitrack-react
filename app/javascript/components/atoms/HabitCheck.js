// Default Imports
import React from 'react'
import PropTypes from 'prop-types'
// Material-UI Imports
import { Checkbox } from 'material-ui'

// Needs date attr, t/f attr

// Component
class HabitCheck extends React.Component {
  render () {
    return (
      <React.Fragment>
        <Checkbox />
      </React.Fragment>
    )
  }
}

export default HabitCheck
